Thanks for the detailed pass — all three items are addressed in the new commits, and I've taken the layout question as a recorded decision rather than leaving it implied.

**1. Anchors re-derived at `555967922`.** I verified each by fetching the files at that exact SHA: `get_kv_cache_shape` is `rocm_attn.py:247-256` (the `%16` check at `:249-251`); `_get_backend_priorities` runs `rocm.py:407-441` with the AITER gates `is_mha_enabled()` (`:434`) and `is_aiter_found_and_supported()` (`:436`); `rocm.py:661-665` is indeed the separate `get_vit_attn_backend` (`on_gfx9()` at `:663`), not part of `_get_backend_priorities`. All citations in `rocm.cpp`, `backend.h`, `backend.cpp` and `docs/ROCM.md` are corrected to these.

**2. Dense list mirrored verbatim** — `{ROCM_ATTN, ROCM_AITER_FA, ROCM_AITER_UNIFIED_ATTN, TRITON_ATTN, TURBOQUANT}` — and the gfx9 inference is gone entirely. The unregistered names are skipped by the walk, so this costs nothing, as you said.

**3. `use_kv_connector` — recorded, not fielded.** I did not add the `AttnSelectorConfig` field. The reasoning, now in the header + spec §4: upstream's guard protects connector transfer semantics from ROCM_ATTN's *asymmetric native K/V views*; our registered shape is the shared symmetric NHD layout — the same one `FLASH_ATTN` allocates, which upstream does use with connectors. The premise of the guard doesn't exist for this registration. The escape hatch is documented: if a future ROCm kernel adopts the asymmetric layout, the registration flips shape AND the guard becomes load-bearing.

**4. The layout question — my view: keep `ROCM_ATTN` as the one exact tracked exception.** I considered the `FLASH_ATTN` flip, and it would work mechanically. But the name here identifies the kernel family that actually runs — the ROCm paged-attn kernel behind `kPagedAttention`/`kROCM` — so the selection log reports the truth on real silicon, whereas `FLASH_ATTN` would permanently dead-letter `ROCM_ATTN` in the priority list, which reads as "ROCm attention unsupported" when it is in fact supported (with the shared NHD layout). This is recorded as §3 of the spec (`rocm-attn-backend.md`, committed in this PR) with the same escape hatch: it stops being an exception the moment a real upstream-layout ROCm kernel lands.

**Process.** The spec is drafted and committed — `.agents/specs/rocm-attn-backend.md` — with the layout decision, the kv-connector reasoning, the verified anchors, reachability, and `## Owed`. It's a first draft; I'd welcome co-iteration. The lifecycle docs are updated in this PR: `backend-matrix.md` `BACKEND-ROCM` row, `docs/FEATURES.md`, and the `docs/ROCM.md` M3 bullet (the `rocm.py:545`/`rocm.py:407` citations there are corrected too).

**On gating:** agreed and recorded — `test_rocm_backend.cpp` is `VLLM_CPP_HIP`-gated, there's no HIP CI job, and the substantive assertions are contributor-hardware evidence (gfx1151). The only CI-covered assertion is the single `HasAttentionBackend(kROCM, "ROCM_ATTN")` line. A HIP CI leg is in the spec's Owed list as a separate, valuable follow-up — happy to help scope it.

**Reachability** is noted in the body; the runner consumption is #1065, and I agree it deserves its own row/issue/spec rather than riding #41 — proposed row `BACKEND-ATTN-SELECTION-RUNNER` (spec §7). Happy to set that up with you.
